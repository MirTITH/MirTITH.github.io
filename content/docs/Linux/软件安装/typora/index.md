---
title: Typora
type: docs
---

## 字体设置

将 [conf.user.json](conf.user.json) 复制到 `~/.config/Typora/conf/`：

```bash
mkdir -p ~/.config/Typora/conf/
cp conf.user.json ~/.config/Typora/conf/
```

这份配置将标准字体设为 `Noto Sans` / `Noto Serif`，等宽字体设为 `Noto Sans Mono`。